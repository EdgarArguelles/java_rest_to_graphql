query all {
    games {
        id
        name
    }
}

query byID {
    game(id: 3) {
        id
        name
        note
        price
        category {
            name
            minAge
        }
        stores {
            name
            address
        }
    }
}
